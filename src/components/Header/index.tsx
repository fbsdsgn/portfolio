import { Moon } from "phosphor-react";
import { Container } from "../../styles/global";
import { Profile } from "../Profile";

export const Header = () => {
  return (
    <Container>
      <Profile />
    </Container>
  );
};
